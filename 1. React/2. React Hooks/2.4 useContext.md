# useContext Hook

[[2. React Hooks.md]]

---

useContext lets you access context values without prop drilling.

## Basic Usage

```typescript
import { createContext, useContext } from 'react';

// 1. Create context
const ThemeContext = createContext<'light' | 'dark'>('light');

// 2. Provide value
function ThemeProvider({ children }) {
  const [theme, setTheme] = useState<'light' | 'dark'>('light');
  return (
    <ThemeContext.Provider value={theme}>
      {children}
    </ThemeContext.Provider>
  );
}

// 3. Use context
function Component() {
  const theme = useContext(ThemeContext);
  return <div className={theme}>Content</div>;
}

// Usage
<ThemeProvider>
  <Component />
</ThemeProvider>
```

---

## Common Pattern: Custom Hook

```typescript
// Create context
const AuthContext = createContext<AuthContextType | null>(null);

// Custom hook for easy access
function useAuth() {
  const context = useContext(AuthContext);
  if (!context) {
    throw new Error('useAuth must be used within AuthProvider');
  }
  return context;
}

// Provider
function AuthProvider({ children }) {
  const [user, setUser] = useState(null);

  const login = async (email: string, password: string) => {
    const user = await loginAPI(email, password);
    setUser(user);
  };

  return (
    <AuthContext.Provider value={{ user, login }}>
      {children}
    </AuthContext.Provider>
  );
}

// Usage
function Dashboard() {
  const { user, login } = useAuth();  // Easy access!
  return <div>{user?.name}</div>;
}
```

---

## Multiple Contexts

```typescript
const ThemeContext = createContext('light');
const LanguageContext = createContext('en');

function App() {
  const [theme, setTheme] = useState('light');
  const [language, setLanguage] = useState('en');

  return (
    <ThemeContext.Provider value={theme}>
      <LanguageContext.Provider value={language}>
        <MainApp />
      </LanguageContext.Provider>
    </ThemeContext.Provider>
  );
}

function Component() {
  const theme = useContext(ThemeContext);
  const language = useContext(LanguageContext);
  return <div className={theme}>Content in {language}</div>;
}
```

---

## Performance: useCallback + useMemo

Context values that change frequently cause re-renders:

```typescript
// ❌ BAD: Object created every render
function Provider({ children }) {
  const [user, setUser] = useState(null);
  return (
    <UserContext.Provider value={{ user, setUser }}>
      {children}
    </UserContext.Provider>
  );
}

// ✅ GOOD: Memoized value
function Provider({ children }) {
  const [user, setUser] = useState(null);

  const value = useMemo(() => ({ user, setUser }), [user]);

  return (
    <UserContext.Provider value={value}>
      {children}
    </UserContext.Provider>
  );
}
```

---

## vs Props

| Approach | Pros | Cons |
| :-- | :-- | :-- |
| **Props** | Explicit, testable | Prop drilling |
| **Context** | No drilling, global access | Harder to track |

Use **Props** for component-specific data.
Use **Context** for truly global data (theme, auth, language).

---

## See Also

- [[2. React Hooks.md]] - Hooks overview
- [[2.3 useEffect.md]] - useEffect hook
- [[1.9 Context API.md]] - Detailed Context API
- [[4.3 Zustand.md]] - Lightweight alternative to Context for global state

---

[[2. React Hooks.md]]
