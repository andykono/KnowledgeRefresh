# Context API

[[1. React Core Concepts.md]]

---

Share state globally without prop drilling.

## Basic Pattern

```typescript
// 1. Create context
const ThemeContext = createContext<'light' | 'dark'>('light');

// 2. Provide value
function ThemeProvider({ children }) {
  const [theme, setTheme] = useState<'light' | 'dark'>('light');
  return (
    <ThemeContext.Provider value={theme}>
      {children}
    </ThemeContext.Provider>
  );
}

// 3. Use context
function Component() {
  const theme = useContext(ThemeContext);
  return <div className={theme}>Content</div>;
}
```

## Custom Hook Pattern

```typescript
// Easier access with custom hook
function useTheme() {
  const context = useContext(ThemeContext);
  if (!context) throw new Error('useTheme must be in provider');
  return context;
}

// Usage
function Component() {
  const theme = useTheme();
  return <div className={theme}>Content</div>;
}
```

---

## Common Use Cases

- Theme (light/dark mode)
- Language/i18n
- Authentication
- User preferences
- Global notifications

---

## Performance

```typescript
// Memoize value to prevent unnecessary re-renders
function Provider({ children }) {
  const [theme, setTheme] = useState('light');
  const value = useMemo(() => ({ theme, setTheme }), [theme]);
  return (
    <ThemeContext.Provider value={value}>
      {children}
    </ThemeContext.Provider>
  );
}
```

---

## See Also

- [[2. React Hooks/2.4 useContext.md]] - useContext hook
- [[1. React Core Concepts.md]] - Core concepts
- [[4. State Management.md]] - When Context isn't enough

---

[[1. React Core Concepts.md]]
